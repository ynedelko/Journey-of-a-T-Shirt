<center>
<h1>The US imports over <strong>$80 Billion</strong> of apparel each year.</h1>
<br>
<h2>Countries with Highest Apparel Imports to US (2014)</h2>
<%= geo_chart @countries.group(:name).sum(:us_imports), colors: ["#33C1FF", "#008ED6"] %>
<table class="table table-condensed">
  <thead>
    <tr>
      <th>Country</th>
      <th>$ Billion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @import_order_countries.each do |country| %>
      <tr>
        <td><%= link_to country.name, country_path(country) %></td>
        <td><%= country.us_imports %></td>
        <td><%= link_to 'Edit', edit_country_path(country) %> |
            <%= link_to 'Destroy', country, method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<h1>Chances are your T-Shirt came from either China, Vietnam, or Bangladesh</h1>
</center>
<br>
<br>
<%= link_to 'New Country', new_country_path, id: "new_country_link", remote: true, class: "btn btn-sm btn-info" %>
<br>
